{
  "name": "herramientas-de-oficina",
  "private": true,
  "version": "1.0.0",
  "description": "Editor de textos tipo Writer con IA integrada.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVIDOR,EDITOR -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "setup": "npm install && npm run install:all && node scripts/setup-env.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
